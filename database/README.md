# ScrumCat Database

This directory stores database initialization and migration scripts.

Current stage: project scaffold only. `init.sql` only creates the `scrumcat` database. Business tables will be added after the database design document is completed.
